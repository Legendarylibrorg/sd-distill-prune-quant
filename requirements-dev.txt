-r requirements.txt

# Linting / formatting / packaging
ruff>=0.5.0
mypy>=1.8.0
build>=1.0.0

# Security tooling used by CI
detect-secrets>=1.5.0
pip-audit>=2.7.0
pre-commit>=3.6.0

# Tests (kept minimal; heavy GPU tests are not included)
pytest>=7.4.0

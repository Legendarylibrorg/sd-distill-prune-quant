# Core deep-learning stack.
# On Linux/Windows with an NVIDIA GPU, install the matching CUDA wheel first:
#     pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision
# On macOS (CPU/MPS) the default PyPI wheels are correct.
torch>=2.13.0
torchvision>=0.28.0

# Diffusers stack
diffusers>=0.39.0
transformers>=5.14.1
accelerate>=1.14.0
safetensors>=0.8.0

# Image + data utilities
pillow>=12.3.0
numpy>=2.5.1
scipy>=1.18.0
tqdm>=4.68.4

# UI
gradio>=6.20.0

# Quality evaluation
lpips>=0.1.4
# CLIP is installed from a pinned GitHub commit by run.sh / run.ps1:
#   pip install "git+https://github.com/openai/CLIP.git@d05afc436d78f1c48dc0dbf8e5980a9d471f35f6"

# Optional ONNX export
optimum>=2.2.0
onnx>=1.22.0
onnxruntime>=1.27.0

# Optional, only if your CUDA/torch combination supports it
# xformers>=0.0.23

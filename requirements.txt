# Core deep-learning stack.
# On Linux/Windows with an NVIDIA GPU, install the matching CUDA wheel first:
#     pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision
# On macOS (CPU/MPS) the default PyPI wheels are correct.
torch>=2.12.0
torchvision>=0.27.0

# Diffusers stack
diffusers>=0.38.0
transformers>=5.9.0
accelerate>=1.13.0
safetensors>=0.7.0

# Image + data utilities
pillow>=12.2.0
numpy>=2.4.6
scipy>=1.17.1
tqdm>=4.67.3

# UI
gradio>=6.14.0

# Quality evaluation
lpips>=0.1.4
# CLIP is installed from GitHub by run.sh / run.ps1:
#   pip install git+https://github.com/openai/CLIP.git

# Optional ONNX export
optimum>=2.1.0
onnx>=1.21.0
onnxruntime>=1.26.0

# Optional, only if your CUDA/torch combination supports it
# xformers>=0.0.23
